#include <iostream>

using namespace std; // simplifies the code by making use of the standard namespace

int main()
{
    double a = 10;
    double b = 3;
    double c = a / b;
    int x = 1;
    int y = 3;
    int z = x + y;
    cout << "z = " << z << endl
         << "c = " << c;
    return 0;

    // ++ increment operator
    // -- decrement operator
}